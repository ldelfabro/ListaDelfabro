import { EstadoAprobacionPipe } from './estado-aprobacion.pipe';

describe('EstadoAprobacionPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoAprobacionPipe();
    expect(pipe).toBeTruthy();
  });
});
